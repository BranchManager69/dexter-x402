# HTTP port for the facilitator service
PORT=4070

# Pino log level
LOG_LEVEL=info

# Comma-separated list of Solana networks this facilitator supports
# Production setup should remain on solana
FACILITATOR_NETWORKS=solana

# Base58-encoded Solana secret key used as the fee payer when settling payments
SOLANA_PRIVATE_KEY=

# Optional: restrict CORS origins (comma-separated)
# ALLOWED_ORIGINS=https://app.example.com
